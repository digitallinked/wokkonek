---
description: Secrets and security guardrails
alwaysApply: true
---

# Secrets & security

- Never print, store, or commit secrets (service role key, passwords, tokens).
- `.env.local` must remain local-only; `.env.example` is safe to commit.
- Do not weaken Storage privacy; receipts must stay private.
- Prefer signed URLs for any private file access.

